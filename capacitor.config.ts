import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fieldflow.app',
  appName: 'FieldFlow',
  webDir: 'dist/fieldflow-pwa/browser'
};

export default config;
